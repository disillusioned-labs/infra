UPTIME_KUMA_PORT=3002

KUMA_USERNAME=admin
KUMA_PASSWORD=securepassword

# Rootful Podman default. For rootless Podman, set this to the result of:
#   podman info --format '{{.Host.RemoteSocket.Path}}'
PODMAN_SOCKET_PATH=/run/podman/podman.sock
